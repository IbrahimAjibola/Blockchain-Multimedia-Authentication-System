{
  "name": "blockchain-multimedia-auth-system",
  "version": "1.0.0",
  "description": "Blockchain-based multimedia authentication and provenance system",
  "private": true,
  "workspaces": [
    "contracts",
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"pnpm run dev:backend\" \"pnpm run dev:frontend\"",
    "dev:backend": "cd backend && pnpm run dev",
    "dev:frontend": "cd frontend && pnpm run dev",
    "build": "pnpm run build:backend && pnpm run build:frontend",
    "build:backend": "cd backend && pnpm run build",
    "build:frontend": "cd frontend && pnpm run build",
    "test": "pnpm run test:contracts && pnpm run test:backend",
    "test:contracts": "cd contracts && pnpm run test",
    "test:backend": "cd backend && pnpm run test",
    "deploy:contracts": "cd contracts && pnpm run deploy",
    "install:all": "pnpm install && cd contracts && pnpm install && cd ../backend && pnpm install && cd ../frontend && pnpm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
